# 🏗️ Aligo 架构设计文档

> 面试快速翻阅的架构卡片，聚焦设计决策和 tradeoff。

---

## 1. 整体架构：意图驱动的协调调度

```
用户输入
   │
   ▼
┌─────────────────────────────────┐
│     IntentionAgent              │  ← 意图识别 + 任务分解
│  ┌───────────┐ ┌─────────────┐  │
│  │ 正则快速  │ │ LLM 深度    │  │  ← 双层识别（快速路由优先）
│  │ 路由 <1ms │ │ 识别 1-3s   │  │
│  └───────────┘ └─────────────┘  │
└──────────────┬──────────────────┘
               │ agent_schedule (JSON)
               ▼
┌─────────────────────────────────┐
│     OrchestrationAgent          │  ← 协调调度 + 结果聚合
│                                 │
│  按优先级分组:                    │
│  ┌─── P1 (并行) ───┐            │
│  │ train_ticket     │            │  ← asyncio.gather()
│  │ hotel_search     │            │
│  │ info_query       │            │
│  └─────────────────┘            │
│  ┌─── P2 (依赖P1) ─┐           │
│  │ itinerary_plan   │            │  ← 等P1结果
│  └─────────────────┘            │
└──────────────┬──────────────────┘
               │
               ▼
          结果 + 记忆更新
```

---

## 2. 意图识别：双层架构

| 层级 | 机制 | 延迟 | 覆盖率 | 适用场景 |
|------|------|------|--------|----------|
| L1 | 正则快速路由 | <1ms | ~80% | 记账/天气/酒店/汇率/火车票/翻译/记忆查询/餐厅 |
| L2 | LLM 深度识别 | 1-3s | ~20% | 复杂/模糊/多意图/追问/上下文依赖 |

**快速路由的上下文感知：**
- 有对话历史但缺少城市 → 放弃快速路由，交给 LLM 补全
- 示例：历史有"上海酒店"，用户说"帮我查查静安区的" → LLM 能理解这是上海静安区的酒店

---

## 3. 记忆系统：三层架构

```
┌──────────────────────────────────────────┐
│  短期记忆 (ShortTermMemory)               │
│  • 会话级环形缓冲区 (max 10 轮)           │
│  • 纯内存，不持久化                        │
│  • 用途: 上下文连贯性                      │
├──────────────────────────────────────────┤
│  长期记忆 (LongTermMemory)                │
│  • PostgreSQL (生产) / JSON (开发)         │
│  • 存储: 偏好/行程/聊天/费用               │
│  • 用途: 跨会话记忆                        │
├──────────────────────────────────────────┤
│  工作记忆 (Orchestration Context)          │
│  • 单次请求内的中间结果                    │
│  • 子Agent之间的上下文共享                  │
│  • 用途: Agent协作                         │
└──────────────────────────────────────────┘
```

**LLM 摘要机制：**
- 累积 ≥10 条消息后触发后台摘要
- 避免上下文窗口溢出
- 摘要包含：偏好 + 行程历史 + 关键对话

---

## 4. 容错机制

| 组件 | 策略 | 配置 |
|------|------|------|
| 熔断器 | 三态模型: 关闭→半开→打开 | 5次失败熔断, 60s恢复, 2次半开成功 |
| 重试 | 指数退避 | 最多2次, 0.5s→8s |
| JSON解析 | 6种降级策略 | markdown剥离/正则提取/容错解析 |
| 存储 | PostgreSQL→JSON | 环境变量切换, 零依赖 |
| 缓存 | Redis→dict | 自动降级, 无感知 |
| SSE | keepalive | 5s超时保活 |

---

## 5. 插件化 Skill 机制

```
skills/{skill-name}/
├── SKILL.md          # 元数据: 名称/职责/触发条件
└── script/
    └── agent.py      # 实现: AgentBase.reply()
```

**加载策略：**
1. 启动时: `LazyAgentRegistry` 扫描所有 SKILL.md，仅加载元数据
2. 首次调度时: 按需实例化 Agent（懒加载）
3. 已加载的 Agent 缓存在内存中

**扩展方式：** 新增目录即可，零代码修改

---

## 6. API 设计

| 端点 | 方法 | 协议 | 说明 |
|------|------|------|------|
| `/api/chat` | POST | SSE | 流式对话（核心端点） |
| `/api/health` | GET | JSON | 健康检查 |
| `/api/memory/{user_id}` | GET | JSON | 查询记忆 |

**SSE 事件流：**
```
thinking → intention → dispatching → agent_start → agent_result → ... → complete
                    ↘ clarification（追问时中断）
```

---

## 7. 技术选型与 Tradeoff

| 决策 | 选择 | 原因 | Tradeoff |
|------|------|------|----------|
| Agent框架 | AgentScope | 阿里开源, 轻量, 支持async | 生态不如 LangChain |
| 意图识别 | 正则+LLM | 80%请求<1ms | 正则维护成本 |
| 存储 | PG+JSON降级 | 零配置可运行 | JSON不支持高并发 |
| 缓存 | Redis+dict降级 | 无Redis也能跑 | dict不跨进程 |
| 前端 | React 19+Vite 8 | 现代化, HMR快 | 需要Node环境 |
| 流式 | SSE | 比WebSocket简单 | 单向通信 |
